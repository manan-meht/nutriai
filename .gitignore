.DS_Store
node_modules/
.next/
.vercel/
.env*.local
.env
# Any env backup/copy — a live key must never be one `git add -A` away
.env*.bak*
.env.local.*
*.log
*.tsbuildinfo
apps/mobile/*.aab
.expo/
.open-next
.dev.vars
.wrangler

# Supabase CLI scratch
supabase/.temp/
