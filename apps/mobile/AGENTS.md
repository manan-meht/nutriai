# Expo HAS CHANGED

This app is on **Expo SDK 54** (`expo@54`, React Native 0.81). Read the
exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any code — APIs move between SDKs, and the `latest` docs describe a
newer one than this app runs.

Check `package.json` if in doubt; this line is the thing that goes stale.
