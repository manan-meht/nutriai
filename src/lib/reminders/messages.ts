export function buildReminderMessage(firstName: string): string {
  return `Hi ${firstName}! 🍽️ Just a friendly nudge from Tistra Health — if you've eaten recently, send a quick photo or description here so we can keep track. 😊`;
}
